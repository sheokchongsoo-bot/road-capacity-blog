# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code에게 주는 안내서입니다.

## 프로젝트 개요

마크다운(`.md`) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
글쓴이는 `content/` 안에 마크다운만 추가하면, 빌드 과정을 통해 배포 가능한
정적 HTML 사이트가 `dist/`에 생성됩니다.

**핵심 목표**
- 깔끔하고 읽기 좋은 디자인 (본문 가독성이 최우선)
- 라이트/다크 모드 지원 (사용자 선택 + 시스템 설정 존중)
- 모바일 반응형
- **프레임워크 없이** 순수 HTML, CSS, JavaScript로만 구현

## 기술 제약 (중요)

- React/Vue/Svelte 등 **UI 프레임워크 사용 금지**. 순수 HTML/CSS/JS(vanilla)만 사용.
- CSS 프레임워크(Tailwind, Bootstrap 등) 사용 금지. 손으로 작성한 CSS를 사용.
- 마크다운 파싱과 사이트 생성을 위한 최소한의 Node.js 빌드 스크립트는 허용.
  단, 런타임(브라우저)에는 어떤 프레임워크도 포함하지 않음.
- 외부 런타임 의존성은 최소로. 폰트/아이콘도 가능하면 시스템 폰트와 인라인 SVG 사용.

## 디렉토리 구조 (제안)

```
content/          # 원본 마크다운 글 (프론트매터 포함)
  hello-world.md
templates/         # HTML 템플릿 조각 (레이아웃, 글 페이지, 목록)
  layout.html
  post.html
  index.html
src/
  build.js         # 마크다운 → HTML 변환 및 사이트 생성 스크립트
  markdown.js      # 마크다운 파싱 로직
  theme.js         # 다크/라이트 모드 토글 (브라우저에서 실행)
public/
  styles.css       # 전역 스타일 (디자인의 핵심)
dist/              # 빌드 결과물 (배포 대상, git 무시)
```

파일이나 폴더를 새로 만들기 전에 위 구조를 따르고, 벗어날 경우 이유를 남길 것.

## 마크다운 처리

- 각 마크다운 파일 상단에 YAML 프론트매터로 메타데이터를 둔다:
  `title`, `date`, `tags`, `description`, `order` 등.
- 빌드 시 프론트매터를 파싱해 목록 페이지(`index.html`)와 각 글 페이지를 생성한다.
- 코드 블록은 문법 강조를 적용하되, 무거운 런타임 하이라이터보다 빌드 타임
  처리 또는 가벼운 방식을 우선한다.
- **수식**: KaTeX로 **빌드 타임**에 렌더링한다. 인라인은 `$ ... $`, 디스플레이는
  `$$ ... $$`(여러 줄은 `\begin{aligned} ... \end{aligned}`)로 쓴다. 첨자는 여러 글자면
  중괄호로 묶는다(`f_{HV}`, `E_{T1}`). 브라우저 런타임 JS는 없고 `katex.min.css`와
  `fonts/*.woff2`만 로드한다. **주의**: KaTeX의 `\text{}`에는 한글 글리프가 없으므로
  수식 안 단위·설명은 `\text{vph}`처럼 영문/기호만 쓰고, 한글 설명은 수식 밖에 둔다.
  숫자의 천단위 콤마는 `2{,}200`으로 써야 간격이 벌어지지 않는다.
- **도판**: 마크다운 안에 `<figure class="diagram">` + 인라인 SVG로 직접 그린다.
  색은 `currentColor`와 CSS 변수(`var(--accent)`, `var(--text-muted)`, `var(--bg-subtle)`,
  `var(--border)`)만 사용해 다크 모드에 자동 대응시킨다(신호등 색 등 의미색은 고정 hex 허용).
  **주의**: HTML 블록 내부에 빈 줄을 넣으면 블록이 쪼개진다. SVG 텍스트에는 `$`, `**`,
  `~`를 쓰지 않는다(수식·강조·취소선 오작동). `figcaption`으로 설명을 단다.
- **목록 정렬**: `order`(강의 번호 등 정수)가 지정된 글을 먼저 오름차순으로,
  `order`가 없는 글끼리는 날짜 역순으로 정렬한다. 강의 노트는 각 글에 `order`를
  부여해 강의 번호 순서로 노출한다.

### 한국어 마크다운 작성 주의 (marked/GFM 함정)

- **볼드 + 조사**: `**용어(부연)**조사`처럼 볼드가 `)`로 끝나고 바로 한글 조사가
  붙으면 강조가 깨진다. 부연을 볼드 밖으로 빼서 `**용어**(부연)조사`로 쓴다.
- **물결표 범위**: `1,000~1,350`, `A~F` 등에서 `~`가 GFM 취소선으로 오인식될 수
  있다. 범위는 엔대시(`–`)로 쓴다.
- **볼드가 수식/괄호로 끝나고 조사가 붙는 경우**: `**용량 $c=S(g/C)$**와`,
  `**포화차두시간($h$)**이라`처럼 볼드가 인라인 수식이나 `)`로 끝나고 바로 한글
  조사가 오면 강조가 깨진다. 수식·괄호를 볼드 밖으로 빼서 `**용량** $c=S(g/C)$와`,
  `**포화차두시간**($h$)이라`로 쓴다.
- 새 글 작성 후 빌드 결과에서 리터럴 `**`나 의도치 않은 `<del>`이 없는지 확인한다:
  `grep -c '\*\*' dist/posts/<slug>.html`, `grep -o '<del>' dist/posts/<slug>.html`.

## 디자인 원칙

- **가독성 우선**: 본문 최대 너비 약 65~75ch, 넉넉한 행간(line-height 1.6~1.8),
  충분한 여백.
- **테마는 CSS 변수로 관리**: 색상은 `:root`에 CSS 커스텀 프로퍼티로 정의하고
  `[data-theme="dark"]`에서 재정의한다. 색상 값을 코드에 하드코딩하지 말 것.
- **다크 모드**: `prefers-color-scheme`를 기본값으로 존중하되, 사용자가 토글하면
  `localStorage`에 저장해 재방문 시 유지한다. FOUC(깜빡임)를 막기 위해 테마
  적용 스크립트는 `<head>`에서 인라인으로 먼저 실행한다.
- **모바일 반응형**: 모바일 우선으로 작성하고 필요한 곳에만 미디어 쿼리를 추가한다.
  터치 타깃은 충분히 크게, 가로 스크롤이 생기지 않도록 한다.
- 접근성: 시맨틱 HTML 태그 사용, 명도 대비(WCAG AA) 충족, 이미지 alt 제공.

## 코드 컨벤션

- 들여쓰기 2칸, 세미콜론 사용.
- 순수 함수 위주로 빌드 로직을 작성하고, 파일 입출력은 경계에서 처리한다.
- 브라우저 JS는 최소한으로. 필수 인터랙션(테마 토글 등)에만 사용한다.
- CSS는 컴포넌트 단위로 섹션을 구분해 주석으로 표시한다.

## 명령어

> 아직 구현 초기 단계입니다. 스크립트를 추가하면 이 섹션을 실제 명령으로 갱신하세요.

```bash
# 빌드: content/의 마크다운을 dist/로 변환
node src/build.js

# 로컬 미리보기 (예: 내장 정적 서버)
npx serve dist        # 또는 python3 -m http.server -d dist
```

## 작업 시 유의사항

- 새 기능을 추가할 때 프레임워크 도입 충동을 피하고 위의 기술 제약을 지킬 것.
- 디자인 변경 시 라이트/다크 두 테마와 모바일/데스크톱 모두에서 확인할 것.
- 색상·간격 등 디자인 토큰은 CSS 변수에서만 수정하고 개별 규칙에 흩뿌리지 말 것.
